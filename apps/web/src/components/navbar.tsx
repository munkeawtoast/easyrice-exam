import React from 'react';

type Props = {};

const Navbar = (props: Props) => {
  return (
    <nav className="w-full py-4 px-6 text-2xl bg-gray-200 backdrop-blur">
      EASYRICE TEST
    </nav>
  );
};

export default Navbar;
